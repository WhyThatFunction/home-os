# RustFS user for the ssegning.com website (Strapi 5 media uploads).
#
# The provisioning Job reads each Secret key as one key=value user record.
# The double-brace escape is required so Helm emits a literal external-secrets
# expression rather than trying to resolve `.access` itself.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ssegning-rustfs-users
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/name: rustfs
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: ssegning-aws
    kind: ClusterSecretStore
  target:
    name: ssegning-rustfs-users
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        ssegning-site-user: |
          username={{ `{{ .access }}` }}
          password={{ `{{ .secret }}` }}
          disabled=false
          policies=ssegning-policy
  data:
    - secretKey: access
      remoteRef:
        key: ssegning/prod/env
        property: s3_access_key
    - secretKey: secret
      remoteRef:
        key: ssegning/prod/env
        property: s3_secret_key
