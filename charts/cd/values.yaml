argoCdConfigs:
  secretMap:
    stephane-segning-home-os-repo:
      type: repository
      data:
        url: "https://github.com/whythatfunction/home-os"
        name: home-os
        type: git

projects:
  - name: external
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: External Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: arc-runner
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Arc Runner
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        name: home-remote
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: mboko
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Mboko Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "mboko-*"
        server: https://kubernetes.default.svc
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: kyc
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: KYC Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "kyc-*"
        server: https://kubernetes.default.svc
      - namespace: "kyc-*"
        name: home-remote
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: keycloak
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Keycloak Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "keycloak"
        server: https://kubernetes.default.svc
      - namespace: "keycloak"
        name: home-remote
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: redis
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Redis Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "redis-system"
        name: home-remote
      - namespace: "redis-system"
        server: https://kubernetes.default.svc
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: infrastructure
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Infrastructure Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
      - namespace: "*"
        name: home-remote
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: ai
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: AI Applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
      - namespace: "*"
        name: "home-remote"
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"
  - name: base-apps
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Random normal applications
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"

  - name: vaam-store
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Vaam Store
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"

  - name: home-remote-init
    additionalLabels: { }
    additionalAnnotations: { }
    permitOnlyProjectScopedClusters: false
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    description: Home Remote
    sourceRepos:
      - "*"
    destinations:
      - namespace: "*"
        server: https://kubernetes.default.svc
      - namespace: "*"
        name: home-remote
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    clusterResourceBlacklist: [ ]
    namespaceResourceBlacklist: [ ]
    namespaceResourceWhitelist:
      - group: "*"
        kind: "*"
    orphanedResources: { }
    roles: [ ]
    syncWindows: [ ]
    sourceNamespaces:
      - "*"

applications:
  - name: cd
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/cd
    destination:
      server: https://kubernetes.default.svc
      namespace: argocd
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: home-secrets
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/WhyThatFunction/home-secrets
      targetRevision: HEAD
      path: manifests
    destination:
      server: https://kubernetes.default.svc
      namespace: argocd
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: storage
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/storage
    destination:
      server: https://kubernetes.default.svc
      namespace: kube-system
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: gateway-api-crds
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/kubernetes-sigs/gateway-api.git
      targetRevision: v1.5.1
      path: config/crd
    destination:
      server: https://kubernetes.default.svc
      namespace: kube-system
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: nvidia
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://nvidia.github.io/k8s-device-plugin
      targetRevision: 0.19.1
      chart: nvidia-device-plugin
      helm:
        valuesObject:
          runtimeClassName: nvidia
          nodeSelector:
            gpu-node: "true"
    destination:
      server: https://kubernetes.default.svc
      namespace: kube-system
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: cloudnative-pg
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://charts.bitnami.com/bitnami
      targetRevision: 1.0.11
      chart: cloudnative-pg
      helm:
        valuesObject:
          global:
            defaultStorageClass: longhorn-local
            security:
              allowInsecureImages: true
          operator:
            image:
              registry: bitnamilegacy
              repository: cloudnative-pg
            postgresqlImage:
              registry: bitnamilegacy
              repository: postgresql
          pluginBarmanCloud:
            enabled: true
            image:
              repository: bitnamilegacy/plugin-barman-cloud
            sidecarImage:
              repository: bitnamilegacy/plugin-barman-cloud-sidecar
    destination:
      server: https://kubernetes.default.svc
      namespace: cloudnative-pg
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: traefik
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://traefik.github.io/charts
      targetRevision: 40.2.0
      chart: traefik
      helm:
        valuesObject:
          additionalArguments:
            - "--api.dashboard=true"
            - "--api.insecure=true"
            - "--serversTransport.insecureSkipVerify=true"
            - "--log.level=ERROR"
            - "--entryPoints.web.http.redirections.entryPoint.to=:443"
            - "--entryPoints.web.http.redirections.entryPoint.scheme=https"
            - "--entryPoints.web.http2.maxConcurrentStreams=1000"
            - "--entryPoints.websecure.http2.maxConcurrentStreams=1000"
            - "--global.sendAnonymousUsage=false"
            # Uncomment the next line if you want to use your self-signed cert for Traefik dashboard
            # - "--entrypoints.websecure.http.tls.domains[0].main=*.home.ssegning"
          logs:
            access:
              enabled: false
          deployment:
            replicas: 3
          service:
            loadBalancerIP: 10.10.0.140
          ports:
            web:
              forwardedHeaders:
                trustedIPs:
                  - 10.10.0.0/24
            websecure:
              forwardedHeaders:
                trustedIPs:
                  - 10.10.0.0/24
            traefik:
              expose:
                web: true
                websecure: true
          providers:
            kubernetesCRD:
              allowCrossNamespace: true
            kubernetesGateway:
              enabled: true
              experimentalChannel: false
            #knative:
            #  enabled: true

          #experimental:
          #  knative: true

          gateway:
            namespacePolicy: All
            listeners:
              web:
                namespacePolicy:
                  from: All
              websecure:
                port: 8443
                protocol: HTTPS
                namespacePolicy:
                  from: All
                certificateRefs:
                  - name: 'wildcard-sls-ssegning-com-tls'
                    kind: Secret
                    group: ''
                mode: Terminate
          
          ingressRoute:
            dashboard:
              enabled: true
              entryPoints:
                - web
                - websecure
              matchRule: Host(`traefik.home.ssegning`)
              tls:
                secretName: traefik.home.ssegning-tls
          
          extraObjects:
            - |
              apiVersion: cert-manager.io/v1
              kind: Certificate
              metadata:
                name: traefik-certificate
              spec:
                secretName: traefik.home.ssegning-tls
                issuerRef:
                  kind: ClusterIssuer
                  name: self-signed-ca
                  group: cert-manager.io
                commonName: traefik.home.ssegning
                dnsNames: 
                  - traefik.home.ssegning
            - |
              apiVersion: cert-manager.io/v1
              kind: Certificate
              metadata:
                name: wildcard-sls-ssegning-com-certificate
              spec:
                secretName: wildcard-sls-ssegning-com-tls
                issuerRef:
                  kind: ClusterIssuer
                  name: cert-cloudflare
                  group: cert-manager.io
                #commonName: sls.ssegning.com
                dnsNames: 
                  - "*.ssegning.com"
                  #- sls.ssegning.com
    destination:
      server: https://kubernetes.default.svc
      namespace: kube-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
      automated:
        prune: true
        selfHeal: true

  - name: cert
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/cert
    destination:
      server: https://kubernetes.default.svc
      namespace: kube-system
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: cert-remote
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/cert
      helm:
        releaseName: cert
        valuesObject:
          cert-manager:
            tolerations:
              - key: node-role.kubernetes.io/control-plane
                operator: Exists
                effect: NoSchedule
    destination:
      name: home-remote
      namespace: kube-system
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: cnpg
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://cloudnative-pg.github.io/charts
      targetRevision: 0.28.2
      chart: cloudnative-pg
      helm:
        releaseName: cnpg
        valuesObject:
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
          resources:
            limits:
              cpu: 100m
              memory: 200Mi
            requests:
              cpu: 100m
              memory: 100Mi
    destination:
      name: home-remote
      namespace: cnpg-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: cnpg-barman-cloud
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://cloudnative-pg.github.io/charts
      targetRevision: 0.5.0
      chart: plugin-barman-cloud
      helm:
        releaseName: cnpg-barman-cloud
        valuesObject:
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
    destination:
      name: home-remote
      namespace: cnpg-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: local-kube-state-metrics
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://prometheus-community.github.io/helm-charts
      targetRevision: "*"
      chart: kube-state-metrics
    destination:
      server: https://kubernetes.default.svc
      namespace: observability
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: s3
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/s3
    destination:
      server: https://kubernetes.default.svc
      namespace: s3
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: apps
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: base-apps
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/apps
    destination:
      server: https://kubernetes.default.svc
      namespace: argocd
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: secret-store
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/home-apps/secret-store
    destination:
      name: home-remote
      namespace: secret-store-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: knative-operator
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://knative.github.io/operator
      targetRevision: v1.22.1
      chart: knative-operator
      helm:
        valuesObject:
          knative_operator:
            knative_operator:
              tolerations:
                - key: node-role.kubernetes.io/control-plane
                  operator: Exists
                  effect: NoSchedule
            operator_webhook:
              tolerations:
                - key: node-role.kubernetes.io/control-plane
                  operator: Exists
                  effect: NoSchedule
    destination:
      name: home-remote
      namespace: knative-operator
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
        - CreateNamespace=true
      automated:
        prune: true
        selfHeal: true

  - name: local-knative-operator
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://knative.github.io/operator
      targetRevision: v1.22.1
      chart: knative-operator
      helm:
        releaseName: knative-operator
    destination:
      server: https://kubernetes.default.svc
      namespace: knative-operator
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
        - CreateNamespace=true
      automated:
        prune: true
        selfHeal: true

  - name: external-secrets
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://charts.external-secrets.io
      targetRevision: v2.5.0
      chart: external-secrets
      helm:
        valuesObject:
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
          webhook:
            certManager:
              enabled: true
              cert:
                create: true
                issuerRef:
                  kind: ClusterIssuer
                  name: self-signed-ca
                  group: cert-manager.io
    destination:
      name: home-remote
      namespace: external-secrets
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
        - CreateNamespace=true
      automated:
        prune: true
        selfHeal: true

  # ESO on the local (homeos) cluster — same chart/version as the home-remote
  # `external-secrets` app. Needed because vymalo-shop runs on homeos (Knative)
  # and pulls its secrets via ExternalSecrets. The self-signed-ca ClusterIssuer
  # already exists on homeos.
  - name: local-external-secrets
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://charts.external-secrets.io
      targetRevision: v2.5.0
      chart: external-secrets
      helm:
        releaseName: external-secrets
        valuesObject:
          webhook:
            certManager:
              enabled: true
              cert:
                create: true
                issuerRef:
                  kind: ClusterIssuer
                  name: self-signed-ca
                  group: cert-manager.io
    destination:
      server: https://kubernetes.default.svc
      namespace: external-secrets
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
        - CreateNamespace=true
      automated:
        prune: true
        selfHeal: true

  # The ssegning-aws ClusterSecretStore on homeos (same chart as the
  # home-remote `secret-store` app). Requires the `awssm-secret` AWS-creds
  # Secret in the secret-store-system namespace on homeos (provisioned
  # out-of-band, e.g. via the home-secrets app).
  - name: local-secret-store
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/home-apps/secret-store
      helm:
        valuesObject:
          secretStores:
            vymalo-secret-store:
              aws:
                service: SecretsManager
                region: eu-central-1
                auth:
                  secretRef:
                    accessKeyIDSecretRef:
                      name: awssm-secret
                      key: access-key
                      namespace: secret-store-system
                    secretAccessKeySecretRef:
                      name: awssm-secret
                      key: secret-access-key
                      namespace: secret-store-system
            vaam-secret-store:
              aws:
                service: SecretsManager
                region: eu-central-1
                auth:
                  secretRef:
                    accessKeyIDSecretRef:
                      name: awssm-secret
                      key: access-key
                      namespace: secret-store-system
                    secretAccessKeySecretRef:
                      name: awssm-secret
                      key: secret-access-key
                      namespace: secret-store-system
    destination:
      server: https://kubernetes.default.svc
      namespace: secret-store-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: keycloak-operator
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://github.com/whythatfunction/home-os
      targetRevision: HEAD
      path: charts/home-apps/keycloak-operator
    destination:
      name: home-remote
      namespace: keycloak-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: mail
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    sources:
      - repoURL: https://bokysan.github.io/docker-postfix
        targetRevision: 5.1.0
        chart: mail
        helm:
          valuesObject:
            existingSecret: mail-config
            config:
              general:
                TZ: "Europe/Berlin"
                ANONYMIZE_EMAILS: "smart"
            persistence:
              enabled: true
      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              mail-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 1h0m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: mail-config
                      creationPolicy: Owner
                    data:
                      - secretKey: ALLOWED_SENDER_DOMAINS
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: allowed_sender_domains
                      - secretKey: RELAYHOST
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost
                      - secretKey: RELAYHOST_PASSWORD
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost_password
                      - secretKey: RELAYHOST_USERNAME
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost_username
                      - secretKey: SMTPD_SASL_USERS
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: smtpd_sasl_users

    destination:
      name: home-remote
      namespace: mail-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: local-mail
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    sources:
      - repoURL: https://bokysan.github.io/docker-postfix
        targetRevision: 5.1.0
        chart: mail
        helm:
          releaseName: mail
          valuesObject:
            existingSecret: mail-config
            config:
              general:
                TZ: "Europe/Berlin"
                ANONYMIZE_EMAILS: "smart"
            persistence:
              enabled: true
      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              mail-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 1h0m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: mail-config
                      creationPolicy: Owner
                    data:
                      - secretKey: ALLOWED_SENDER_DOMAINS
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: allowed_sender_domains
                      - secretKey: RELAYHOST
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost
                      - secretKey: RELAYHOST_PASSWORD
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost_password
                      - secretKey: RELAYHOST_USERNAME
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: relayhost_username
                      - secretKey: SMTPD_SASL_USERS
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: smtpd_sasl_users

    destination:
      server: https://kubernetes.default.svc
      namespace: mail-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  # Apprise notification gateway (stateless). The chain is
  # app (medusa/auth in ns vymalo) -> apprise (/notify) -> postfix (ns
  # mail-system) -> SMTP. Callers pass the full apprise URL (e.g. a mailto://
  # to the postfix relay) in the POST body, so there's NO config/state and NO
  # credentials to hold here — locked down by a NetworkPolicy instead:
  # ingress only from ns vymalo, egress only to DNS + the postfix relay.
  # In-cluster (same cluster as vymalo + local-mail).
  - name: apprise
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    sources:
      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          releaseName: apprise
          valuesObject:
            controllers:
              apprise:
                strategy: RollingUpdate
                containers:
                  app:
                    image:
                      repository: caronc/apprise
                      tag: latest
                    env:
                      # gunicorn's default worker count scales with the node's
                      # CPU count ((2*cores)+1), and each apprise worker loads
                      # the full plugin set (~100Mi) — on a many-core node that
                      # OOMKilled the pod. This is a low-volume transactional
                      # gateway, so pin a small fixed count.
                      APPRISE_WORKER_COUNT: "2"
                    probes:
                      liveness:
                        enabled: true
                        custom: true
                        spec:
                          tcpSocket: { port: 8000 }
                          initialDelaySeconds: 10
                          periodSeconds: 20
                      readiness:
                        enabled: true
                        custom: true
                        spec:
                          tcpSocket: { port: 8000 }
                          initialDelaySeconds: 5
                          periodSeconds: 10
                      startup:
                        enabled: false
                    resources:
                      requests:
                        cpu: 10m
                        memory: 128Mi
                      limits:
                        # 2 workers × ~100Mi + master, with headroom.
                        memory: 384Mi
            service:
              app:
                controller: apprise
                ports:
                  http:
                    port: 8000
            rawResources:
              netpol:
                enabled: true
                apiVersion: networking.k8s.io/v1
                kind: NetworkPolicy
                spec:
                  spec:
                    podSelector:
                      matchLabels:
                        app.kubernetes.io/name: apprise
                    policyTypes:
                      - Ingress
                      - Egress
                    ingress:
                      # Only the vymalo apps may trigger notifications.
                      - from:
                          - namespaceSelector:
                              matchLabels:
                                kubernetes.io/metadata.name: vymalo
                        ports:
                          - protocol: TCP
                            port: 8000
                    egress:
                      # DNS resolution.
                      - to:
                          - namespaceSelector:
                              matchLabels:
                                kubernetes.io/metadata.name: kube-system
                        ports:
                          - protocol: UDP
                            port: 53
                          - protocol: TCP
                            port: 53
                      # The postfix relay (submission + smtp). Future external
                      # channels (e.g. WhatsApp) would add a 443 egress rule.
                      - to:
                          - namespaceSelector:
                              matchLabels:
                                kubernetes.io/metadata.name: mail-system
                        ports:
                          - protocol: TCP
                            port: 587
                          - protocol: TCP
                            port: 25
    destination:
      server: https://kubernetes.default.svc
      namespace: notification-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  # ai-apps (old Linode gen, targetRevision HEAD) REMOVED 2026-06-06 — the
  # Linode cluster was deleted; the ai-* generation is decommissioned. Only
  # ai-apps-v2 (Hetzner) remains. (The lke560142-ctx project destination was
  # dropped above too.)
  - name: ai-apps-v2
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: ai
    source:
      repoURL: https://github.com/ADORSYS-GIS/ai-helm
      targetRevision: release-2026.06.19-v05
      path: charts/apps
    destination:
      server: https://kubernetes.default.svc
      namespace: argocd
    syncPolicy:
      automated:
        prune: true
        selfHeal: true

  - name: arc-runners
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    source:
      repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set-controller"
      targetRevision: 0.13.1
      chart: "."
      helm:
        releaseName: arc
        valuesObject:
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
    destination:
      name: home-remote
      namespace: arc-systems
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: sky-runners-bff
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-sky
          valuesObject:
            githubConfigUrl: https://github.com/skyengpro/azamra-tokenization-bff
            githubConfigSecret: gh-runner-secret-pat

            maxRunners: 2

            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                # fsGroup makes the emptyDir volumes group-owned by 1001 and
                # group-writable, so the uid-1001 runner can mkdir inside the
                # tool cache mounted at /opt/hostedtoolcache (ruby/setup-ruby
                # hardcodes that path and ignores RUNNER_TOOL_CACHE).
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: dind
                    image: docker:29-dind
                    resources:
                      requests:
                        cpu: 250m
                        memory: 512Mi
                      limits:
                        cpu: "2"
                        memory: 4Gi
                    securityContext:
                      privileged: true
                    env:
                      - name: DOCKER_TLS_CERTDIR
                        value: ""
                    args:
                      - dockerd
                      - --host=tcp://0.0.0.0:2375
                      - --host=unix:///var/run/docker.sock
                    startupProbe:
                      tcpSocket:
                        port: 2375
                      failureThreshold: 24
                      periodSeconds: 5
                    volumeMounts:
                      - name: docker-graph
                        mountPath: /var/lib/docker
                      - name: work
                        mountPath: /home/runner/_work

                  - name: runner
                    image: ghcr.io/vymalo/arc-runners:4063075
                    resources:
                      requests:
                        cpu: 500m
                        memory: 1Gi
                      limits:
                        cpu: "4"
                        memory: 8Gi
                    command: [ "/bin/sh", "-c" ]
                    args:
                      - sleep 15; exec /home/runner/run.sh
                    env:
                      - name: DOCKER_HOST
                        value: tcp://localhost:2375
                      # ruby/setup-ruby always installs under /opt/hostedtoolcache
                      # on Linux, so anchor the whole tool cache there to share
                      # one volume across setup-node/java/ruby.
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                      - name: SCCACHE_BUCKET
                        value: sc-cache
                      - name: SCCACHE_ENDPOINT
                        value: s3.ssegning.me
                      - name: TURBO_API
                        value: http://turbo-cache.turbo-cache.svc.cluster.local
                      - name: TURBO_TEAM
                        value: vaam
                      - name: TURBO_TOKEN
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: turbo-token } }
                      - name: SCCACHE_S3_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: SCCACHE_S3_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                      - name: AWS_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: AWS_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache

                volumes:
                  - name: docker-graph
                    emptyDir: { }
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

    destination:
      name: home-remote
      namespace: gh-runners-sky
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: sky-runners-frontend
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-sky-frontend
          valuesObject:
            githubConfigUrl: https://github.com/skyengpro/azamra-tokenization-frontend
            githubConfigSecret: gh-runner-secret-pat

            maxRunners: 2

            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                # fsGroup makes the emptyDir volumes group-owned by 1001 and
                # group-writable, so the uid-1001 runner can mkdir inside the
                # tool cache mounted at /opt/hostedtoolcache (ruby/setup-ruby
                # hardcodes that path and ignores RUNNER_TOOL_CACHE).
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: dind
                    image: docker:29-dind
                    resources:
                      requests:
                        cpu: 250m
                        memory: 512Mi
                      limits:
                        cpu: "2"
                        memory: 4Gi
                    securityContext:
                      privileged: true
                    env:
                      - name: DOCKER_TLS_CERTDIR
                        value: ""
                    args:
                      - dockerd
                      - --host=tcp://0.0.0.0:2375
                      - --host=unix:///var/run/docker.sock
                    startupProbe:
                      tcpSocket:
                        port: 2375
                      failureThreshold: 24
                      periodSeconds: 5
                    volumeMounts:
                      - name: docker-graph
                        mountPath: /var/lib/docker
                      - name: work
                        mountPath: /home/runner/_work

                  - name: runner
                    image: ghcr.io/vymalo/arc-runners:4063075
                    resources:
                      requests:
                        cpu: 500m
                        memory: 1Gi
                      limits:
                        cpu: "4"
                        memory: 8Gi
                    command: [ "/home/runner/run.sh" ]
                    env:
                      - name: DOCKER_HOST
                        value: tcp://localhost:2375
                      # ruby/setup-ruby always installs under /opt/hostedtoolcache
                      # on Linux, so anchor the whole tool cache there to share
                      # one volume across setup-node/java/ruby.
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                      - name: SCCACHE_BUCKET
                        value: sc-cache
                      - name: SCCACHE_ENDPOINT
                        value: s3.ssegning.me
                      - name: TURBO_API
                        value: http://turbo-cache.turbo-cache.svc.cluster.local
                      - name: TURBO_TEAM
                        value: vaam
                      - name: TURBO_TOKEN
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: turbo-token } }
                      - name: SCCACHE_S3_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: SCCACHE_S3_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                      - name: AWS_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: AWS_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache
                volumes:
                  - name: docker-graph
                    emptyDir: { }
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              auth-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: gh-runner-secret-pat
                      creationPolicy: Owner
                    data:
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
              arc-runner-secrets:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: arc-runner-secrets
                      creationPolicy: Owner
                    data:
                      - secretKey: turbo-token
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: turbo_token
                      - secretKey: access-key-id
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_access_key
                      - secretKey: secret-access-key
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_secret_key
    destination:
      name: home-remote
      namespace: gh-runners-sky
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: vaam-runners
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-vaam
          valuesObject:
            githubConfigUrl: https://github.com/vaam-store
            githubConfigSecret: gh-runner-secret-pat

            maxRunners: 4
            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                # fsGroup makes the emptyDir volumes group-owned by 1001 and
                # group-writable, so the uid-1001 runner can mkdir inside the
                # tool cache mounted at /opt/hostedtoolcache (ruby/setup-ruby
                # hardcodes that path and ignores RUNNER_TOOL_CACHE).
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: dind
                    image: docker:29-dind
                    resources:
                      requests:
                        cpu: 250m
                        memory: 512Mi
                      limits:
                        cpu: "2"
                        memory: 4Gi
                    securityContext:
                      privileged: true
                    env:
                      - name: DOCKER_TLS_CERTDIR
                        value: ""
                    args:
                      - dockerd
                      - --host=tcp://0.0.0.0:2375
                      - --host=unix:///var/run/docker.sock
                    startupProbe:
                      tcpSocket:
                        port: 2375
                      failureThreshold: 24
                      periodSeconds: 5
                    volumeMounts:
                      - name: docker-graph
                        mountPath: /var/lib/docker
                      - name: work
                        mountPath: /home/runner/_work

                  - name: runner
                    image: ghcr.io/vymalo/arc-runners:4063075
                    resources:
                      requests:
                        cpu: 500m
                        memory: 2Gi
                      limits:
                        cpu: "4"
                        memory: 8Gi
                    command: [ "/bin/sh", "-c" ]
                    args:
                      - sleep 15; exec /home/runner/run.sh
                    env:
                      - name: DOCKER_HOST
                        value: tcp://localhost:2375
                      # ruby/setup-ruby always installs under /opt/hostedtoolcache
                      # on Linux, so anchor the whole tool cache there to share
                      # one volume across setup-node/java/ruby.
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                      - name: SCCACHE_BUCKET
                        value: sc-cache
                      - name: SCCACHE_ENDPOINT
                        value: s3.ssegning.me
                      - name: TURBO_API
                        value: http://turbo-cache.turbo-cache.svc.cluster.local
                      - name: TURBO_TEAM
                        value: vaam
                      - name: TURBO_TOKEN
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: turbo-token } }
                      - name: SCCACHE_S3_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: SCCACHE_S3_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                      - name: AWS_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: AWS_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache
                volumes:
                  - name: docker-graph
                    emptyDir: { }
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              auth-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: gh-runner-secret-pat
                      creationPolicy: Owner
                    data:
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
              arc-runner-secrets:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: arc-runner-secrets
                      creationPolicy: Owner
                    data:
                      - secretKey: turbo-token
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: turbo_token
                      - secretKey: access-key-id
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_access_key
                      - secretKey: secret-access-key
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_secret_key
              gh-pull-token:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: ghcr-pull-secret
                      creationPolicy: Owner
                      template:
                        engineVersion: v2
                        type: kubernetes.io/dockerconfigjson
                        data:
                          .dockerconfigjson: |
                            {
                              "auths": {
                                "ghcr.io": {
                                  "username": "{{ `{{ .github_username }}` }}",
                                  "password": "{{ `{{ .github_token }}` }}",
                                  "email": "{{ `{{ .github_email }}` }}",
                                  "auth": "{{ `{{ printf "%s:%s" .github_username .github_token | b64enc }}` }}"
                                }
                              }
                            }
                    data:
                      - secretKey: github_username
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_username
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
                      - secretKey: github_email
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_email
    destination:
      name: home-remote
      namespace: gh-runners-vaam
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: gis-runners
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-sse
          valuesObject:
            githubConfigUrl: https://github.com/ADORSYS-GIS
            githubConfigSecret: gh-runner-secret-pat
            # Shadow the GitHub-hosted `ubuntu-latest` label, but scoped to the
            # `flaky-rescue` runner group: only repos in that group's access
            # policy route here; every other repo still hits GitHub-hosted.
            runnerScaleSetName: ubuntu-latest
            runnerGroup: flaky-rescue

            maxRunners: 4
            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                # fsGroup makes the emptyDir volumes group-owned by 1001 and
                # group-writable, so the uid-1001 runner can mkdir inside the
                # tool cache mounted at /opt/hostedtoolcache (ruby/setup-ruby
                # hardcodes that path and ignores RUNNER_TOOL_CACHE).
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: dind
                    image: docker:29-dind
                    resources:
                      requests:
                        cpu: 250m
                        memory: 512Mi
                      limits:
                        cpu: "1"
                        memory: 3Gi
                    securityContext:
                      privileged: true
                    env:
                      - name: DOCKER_TLS_CERTDIR
                        value: ""
                    args:
                      - dockerd
                      - --host=tcp://0.0.0.0:2375
                      - --host=unix:///var/run/docker.sock
                    startupProbe:
                      tcpSocket:
                        port: 2375
                      failureThreshold: 24
                      periodSeconds: 5
                    volumeMounts:
                      - name: docker-graph
                        mountPath: /var/lib/docker
                      - name: work
                        mountPath: /home/runner/_work

                  - name: runner
                    image: ghcr.io/actions/actions-runner:latest
                    resources:
                      requests:
                        cpu: 500m
                        memory: 1Gi
                      limits:
                        cpu: "2"
                        memory: 7Gi
                    command: [ "/bin/sh", "-c" ]
                    args:
                      - sleep 30; exec /home/runner/run.sh
                    env:
                      - name: DOCKER_HOST
                        value: tcp://localhost:2375
                      # ruby/setup-ruby always installs under /opt/hostedtoolcache
                      # on Linux, so anchor the whole tool cache there to share
                      # one volume across setup-node/java/ruby.
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                      - name: SCCACHE_BUCKET
                        value: sc-cache
                      - name: SCCACHE_ENDPOINT
                        value: s3.ssegning.me
                      - name: TURBO_API
                        value: http://turbo-cache.turbo-cache.svc.cluster.local
                      - name: TURBO_TEAM
                        value: vaam
                      - name: TURBO_TOKEN
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: turbo-token } }
                      - name: SCCACHE_S3_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: SCCACHE_S3_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                      - name: AWS_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: AWS_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache
                volumes:
                  - name: docker-graph
                    emptyDir: { }
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              auth-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: gh-runner-secret-pat
                      creationPolicy: Owner
                    data:
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
              arc-runner-secrets:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: arc-runner-secrets
                      creationPolicy: Owner
                    data:
                      - secretKey: turbo-token
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: turbo_token
                      - secretKey: access-key-id
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_access_key
                      - secretKey: secret-access-key
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_secret_key
              gh-pull-token:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: ghcr-pull-secret
                      creationPolicy: Owner
                      template:
                        engineVersion: v2
                        type: kubernetes.io/dockerconfigjson
                        data:
                          .dockerconfigjson: |
                            {
                              "auths": {
                                "ghcr.io": {
                                  "username": "{{ `{{ .github_username }}` }}",
                                  "password": "{{ `{{ .github_token }}` }}",
                                  "email": "{{ `{{ .github_email }}` }}",
                                  "auth": "{{ `{{ printf "%s:%s" .github_username .github_token | b64enc }}` }}"
                                }
                              }
                            }
                    data:
                      - secretKey: github_username
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_username
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
                      - secretKey: github_email
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_email

    destination:
      name: home-remote
      namespace: gh-runners-gis
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  # Self-hosted runners for the vymalo org (CI for vymalo-shop & co.). Org-level
  # registration like vaam/gis. runs-on label = the scale-set name:
  # `arc-runner-set-vymalo`. Starts on the stock actions-runner image; once the
  # project base image (ghcr.io/vymalo/vymalo-arc-runner, built by the
  # vymalo-shop runner-image workflow with node+pnpm+deps pre-fetched) exists,
  # swap the `runner` container image to it (imagePullSecret already wired).
  - name: vymalo-runners
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-vymalo
          valuesObject:
            githubConfigUrl: https://github.com/vymalo
            githubConfigSecret: gh-runner-secret-pat

            maxRunners: 4
            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                # fsGroup makes the emptyDir volumes group-owned by 1001 and
                # group-writable, so the uid-1001 runner can mkdir inside the
                # tool cache mounted at /opt/hostedtoolcache (ruby/setup-ruby
                # hardcodes that path and ignores RUNNER_TOOL_CACHE).
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: dind
                    image: docker:29-dind
                    resources:
                      requests:
                        cpu: 250m
                        memory: 512Mi
                      limits:
                        cpu: "2"
                        memory: 4Gi
                    securityContext:
                      privileged: true
                    env:
                      - name: DOCKER_TLS_CERTDIR
                        value: ""
                    args:
                      - dockerd
                      - --host=tcp://0.0.0.0:2375
                      - --host=unix:///var/run/docker.sock
                    startupProbe:
                      tcpSocket:
                        port: 2375
                      failureThreshold: 24
                      periodSeconds: 5
                    volumeMounts:
                      - name: docker-graph
                        mountPath: /var/lib/docker
                      - name: work
                        mountPath: /home/runner/_work

                  - name: runner
                    # Shared batteries-included runner image (Node + pnpm + Rust +
                    # Flutter + Android + Ruby + k8s tooling), built & published by
                    # github.com/vymalo/arc-runners — same build sky/vaam use. This
                    # also serves the Flutter mobile CI (mobile-ci.yml runs-on this
                    # default scale-set; the separate flutter scale-set is retired).
                    image: ghcr.io/vymalo/arc-runners:4063075
                    # `backend integration tests` boots the full Medusa app + a
                    # live auth NestJS subprocess + jest workers in THIS container
                    # (Postgres is the dind service) — it OOMKilled at 4Gi (GitHub's
                    # ubuntu-latest has ~7Gi). Give it ubuntu-class headroom.
                    resources:
                      requests:
                        cpu: 500m
                        memory: 2Gi
                      limits:
                        cpu: "4"
                        memory: 8Gi
                    command: [ "/bin/sh", "-c" ]
                    args:
                      - sleep 15; exec /home/runner/run.sh
                    env:
                      - name: DOCKER_HOST
                        value: tcp://localhost:2375
                      # ruby/setup-ruby always installs under /opt/hostedtoolcache
                      # on Linux, so anchor the whole tool cache there to share
                      # one volume across setup-node/java/ruby.
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                      - name: SCCACHE_BUCKET
                        value: sc-cache
                      - name: SCCACHE_ENDPOINT
                        value: s3.ssegning.me
                      - name: TURBO_API
                        value: http://turbo-cache.turbo-cache.svc.cluster.local
                      - name: TURBO_TEAM
                        value: vaam
                      - name: TURBO_TOKEN
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: turbo-token } }
                      - name: SCCACHE_S3_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: SCCACHE_S3_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                      - name: AWS_ACCESS_KEY_ID
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: access-key-id } }
                      - name: AWS_SECRET_ACCESS_KEY
                        valueFrom: { secretKeyRef: { name: arc-runner-secrets, key: secret-access-key } }
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache
                volumes:
                  - name: docker-graph
                    emptyDir: { }
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              auth-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: gh-runner-secret-pat
                      creationPolicy: Owner
                    data:
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
              arc-runner-secrets:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: arc-runner-secrets
                      creationPolicy: Owner
                    data:
                      - secretKey: turbo-token
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: turbo_token
                      - secretKey: access-key-id
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_access_key
                      - secretKey: secret-access-key
                        remoteRef:
                          key: "prod/artifact-cache/env"
                          property: object_storage_secret_key
              gh-pull-token:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: ghcr-pull-secret
                      creationPolicy: Owner
                      template:
                        engineVersion: v2
                        type: kubernetes.io/dockerconfigjson
                        data:
                          .dockerconfigjson: |
                            {
                              "auths": {
                                "ghcr.io": {
                                  "username": "{{ `{{ .github_username }}` }}",
                                  "password": "{{ `{{ .github_token }}` }}",
                                  "email": "{{ `{{ .github_email }}` }}",
                                  "auth": "{{ `{{ printf "%s:%s" .github_username .github_token | b64enc }}` }}"
                                }
                              }
                            }
                    data:
                      - secretKey: github_username
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_username
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
                      - secretKey: github_email
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_email
    destination:
      name: home-remote
      namespace: gh-runners-vymalo
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  # FLUTTER mobile CI runner — sibling of vymalo-runners (the Node one). Separate
  # scale-set + namespace so the heavy Flutter+Android image only spins up for
  # `mobile/**` PRs and can't collide with the Node runner's secrets. runs-on
  # label = the scale-set name: `arc-runner-set-vymalo-flutter`. NO DinD —
  # Flutter/Gradle need no Docker. The runner image
  # (ghcr.io/vymalo/vymalo-flutter-runner) is built by the vymalo-shop
  # `flutter runner image` workflow; minRunners 0 means no pod (and no image pull)
  # until a mobile-ci job is queued, so it's safe to deploy before the image exists.
  - name: vymalo-flutter-runners
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: "oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set"
        targetRevision: 0.13.1
        chart: "."
        helm:
          releaseName: arc-runner-set-vymalo-flutter
          valuesObject:
            githubConfigUrl: https://github.com/vymalo
            githubConfigSecret: gh-runner-secret-pat

            maxRunners: 1
            minRunners: 0

            controllerServiceAccount:
              namespace: arc-systems
              name: arc-gha-rs-controller

            template:
              spec:
                securityContext:
                  fsGroup: 1001
                imagePullSecrets:
                  - name: ghcr-pull-secret
                containers:
                  - name: runner
                    # Flutter + Android SDK baked. :latest → pullPolicy Always, so
                    # a rebuilt image rolls in on the next ephemeral pod.
                    image: ghcr.io/vymalo/vymalo-flutter-runner:latest
                    # analyze/test are Dart-only (light); the apk build runs
                    # Gradle/AGP in this container — give it ubuntu-class headroom.
                    resources:
                      requests:
                        cpu: "1"
                        memory: 4Gi
                      limits:
                        cpu: "4"
                        memory: 8Gi
                    env:
                      - name: RUNNER_TOOL_CACHE
                        value: /opt/hostedtoolcache
                    securityContext:
                      runAsUser: 1001
                      runAsGroup: 1001
                    volumeMounts:
                      - name: work
                        mountPath: /home/runner/_work
                      - name: tool-cache
                        mountPath: /opt/hostedtoolcache
                volumes:
                  - name: work
                    emptyDir: { }
                  - name: tool-cache
                    emptyDir: { }

      - repoURL: https://bjw-s-labs.github.io/helm-charts
        targetRevision: 4.6.2
        chart: app-template
        helm:
          valuesObject:
            rawResources:
              auth-secret:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: gh-runner-secret-pat
                      creationPolicy: Owner
                    data:
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
              gh-pull-token:
                enabled: true
                apiVersion: external-secrets.io/v1
                kind: ExternalSecret
                spec:
                  spec:
                    refreshInterval: 0h10m0s
                    secretStoreRef:
                      name: ssegning-aws
                      kind: ClusterSecretStore
                    target:
                      name: ghcr-pull-secret
                      creationPolicy: Owner
                      template:
                        engineVersion: v2
                        type: kubernetes.io/dockerconfigjson
                        data:
                          .dockerconfigjson: |
                            {
                              "auths": {
                                "ghcr.io": {
                                  "username": "{{ `{{ .github_username }}` }}",
                                  "password": "{{ `{{ .github_token }}` }}",
                                  "email": "{{ `{{ .github_email }}` }}",
                                  "auth": "{{ `{{ printf "%s:%s" .github_username .github_token | b64enc }}` }}"
                                }
                              }
                            }
                    data:
                      - secretKey: github_username
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_username
                      - secretKey: github_token
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_token
                      - secretKey: github_email
                        remoteRef:
                          key: "prod/meta/test-app"
                          property: github_email
    destination:
      name: home-remote
      namespace: gh-runners-vymalo-flutter
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  # Self-hosted Turborepo remote cache for the vymalo-shop CI `check` gate. Runs
  # on home-remote alongside the ARC runners so they reach it over cluster DNS
  # (turbo-cache.turbo-cache.svc.cluster.local) — no public route. Multi-source:
  # the private OCI chart + the vymalo-shop prod overlay ($values). Pulls
  # TURBO_TOKEN + MinIO creds from prod/artifact-cache/env via the cluster-wide
  # ssegning-aws ClusterSecretStore (already on home-remote).
  - name: turbo-cache
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: arc-runner
    sources:
      - repoURL: ghcr.io/vymalo/charts
        chart: turbo-cache
        targetRevision: 0.1.*
        helm:
          releaseName: turbo-cache
          valueFiles:
            - $values/deploy/envs/production/turbo-cache.values.yaml
          ignoreMissingValueFiles: true
      - repoURL: https://github.com/vymalo/vymalo-shop.git
        targetRevision: main
        ref: values
    destination:
      name: home-remote
      namespace: turbo-cache
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: otel-operator
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: https://open-telemetry.github.io/opentelemetry-helm-charts
      targetRevision: 0.113.1
      chart: opentelemetry-operator
      helm:
        releaseName: opentelemetry-operator
        valuesObject:
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
          admissionWebhooks:
            autoGenerateCert:
              enabled: false
            certManager:
              issuerRef:
                kind: ClusterIssuer
                name: self-signed-ca
                group: cert-manager.io
    destination:
      name: home-remote
      namespace: opentelemetry-system
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: kserve-crd
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: "oci://ghcr.io/kserve/charts/kserve-crd"
      targetRevision: v0.17.0
      chart: "."
      helm:
        releaseName: kserve-crd
        valuesObject: { }
    destination:
      server: https://kubernetes.default.svc
      namespace: kserve
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: kserve
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: "oci://ghcr.io/kserve/charts/kserve-resources"
      targetRevision: v0.17.0
      chart: "."
      helm:
        releaseName: kserve
        valuesObject: { }
    destination:
      server: https://kubernetes.default.svc
      namespace: kserve
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: knative-serving-local
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: "https://bjw-s-labs.github.io/helm-charts"
      targetRevision: "4.6.2"
      chart: "app-template"
      helm:
        releaseName: knative-serving
        valuesObject:
          rawResources:
            serving:
              apiVersion: operator.knative.dev/v1beta1
              kind: KnativeServing
              spec:
                spec:
                  workloads:
                    - name: net-gateway-api-webhook
                      resources:
                        - container: webhook
                          requests:
                            cpu: 100m
                            memory: 100Mi
                          limits:
                            cpu: 500m
                            memory: 500Mi
                      livenessProbes:
                        - container: webhook
                          initialDelaySeconds: 60
                          periodSeconds: 10
                          failureThreshold: 6
                    - name: controller
                      resources:
                        - container: controller
                          requests:
                            cpu: 300m
                            memory: 100Mi
                          limits:
                            cpu: 1000m
                            memory: 250Mi
                  ingress:
                    gateway-api:
                      enabled: true
                  config:
                    features:
                      kubernetes.podspec-runtimeclassname: "enabled"
                      kubernetes.podspec-nodeselector: "enabled"
                      kubernetes.podspec-persistent-volume-claim: "enabled"
                      kubernetes.podspec-persistent-volume-write: "enabled"
                    domain:
                      sls.ssegning.com: ""
                    gateway:
                      external-gateways: |
                        - class: traefik
                          gateway: kube-system/traefik-gateway
                          service: kube-system/traefik
                      local-gateways: |
                        - class: traefik
                          gateway: kube-system/traefik-gateway
                          service: kube-system/traefik
                    network:
                      ingress-class: gateway-api.ingress.networking.knative.dev
                      domain-template: "{{ `{{ .Name }}` }}-{{ `{{ .Namespace }}` }}--{{ `{{ .Domain }}` }}"
                    certmanager:
                      issuerRef: |
                        kind: ClusterIssuer
                        name: cert-cloudflare
                      clusterLocalIssuerRef: |
                        kind: ClusterIssuer
                        name: self-signed-ca
                      systemInternalIssuerRef: |
                        kind: ClusterIssuer
                        name: self-signed-ca
    destination:
      server: https://kubernetes.default.svc
      namespace: knative-serving
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true

  - name: knative-serving
    finalizers:
      - resources-finalizer.argocd.argoproj.io
    project: infrastructure
    source:
      repoURL: "https://bjw-s-labs.github.io/helm-charts"
      targetRevision: "4.6.2"
      chart: "app-template"
      helm:
        releaseName: knative-serving
        valuesObject:
          rawResources:
            serving:
              apiVersion: operator.knative.dev/v1beta1
              kind: KnativeServing
              spec:
                spec:
                  workloads:
                    - name: net-gateway-api-webhook
                      resources:
                        - container: webhook
                          requests:
                            cpu: 100m
                            memory: 100Mi
                          limits:
                            cpu: 500m
                            memory: 500Mi
                      livenessProbes:
                        - container: webhook
                          initialDelaySeconds: 60
                          periodSeconds: 10
                          failureThreshold: 6
                    - name: controller
                      resources:
                        - container: controller
                          requests:
                            cpu: 300m
                            memory: 100Mi
                          limits:
                            cpu: 1000m
                            memory: 250Mi
                  ingress:
                    gateway-api:
                      enabled: true
                  config:
                    features:
                      kubernetes.podspec-nodeselector: "enabled"
                      kubernetes.podspec-persistent-volume-claim: "enabled"
                      kubernetes.podspec-persistent-volume-write: "enabled"
                    domain:
                      "serverless.vaam.store": ""
                    gateway:
                      external-gateways: |
                        - class: traefik
                          gateway: kube-system/traefik-gateway
                          service: kube-system/traefik
                      local-gateways: |
                        - class: traefik
                          gateway: kube-system/traefik-gateway
                          service: kube-system/traefik
                    network:
                      ingress-class: gateway-api.ingress.networking.knative.dev
                      domain-template: "{{ `{{ .Name }}` }}-{{ `{{ .Namespace }}` }}--{{ `{{ .Domain }}` }}"
                    certmanager:
                      issuerRef: |
                        kind: ClusterIssuer
                        name: cert-home-cert-http
                      clusterLocalIssuerRef: |
                        kind: ClusterIssuer
                        name: self-signed-ca
                      systemInternalIssuerRef: |
                        kind: ClusterIssuer
                        name: self-signed-ca
    destination:
      name: home-remote
      namespace: knative-serving
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
      automated:
        prune: true
        selfHeal: true
