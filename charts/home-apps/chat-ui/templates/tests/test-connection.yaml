apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chat-ui.fullname" . }}-test-connection"
  labels:
    {{- include "chat-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chat-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
