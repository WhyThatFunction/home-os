# Apply AFTER the cluster is up (kubectl apply -f kata-runtimeclass.yaml).
# The kata-containers extension registers the `kata` runtime in containerd;
# this RuntimeClass lets you schedule pods onto it with:
#   spec.runtimeClassName: kata
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata
handler: kata