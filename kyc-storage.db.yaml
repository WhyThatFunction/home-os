apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: kyc-store-pg
  namespace: kyc-store
spec:
  instances: 2
  storage:
    size: 10Gi
    storageClass: longhorn
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "1Gi"
      cpu: "1"